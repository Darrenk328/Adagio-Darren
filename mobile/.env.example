# Your backend's URL (LAN IP if testing on a physical device via Expo Go)
EXPO_PUBLIC_BACKEND_URL=http://localhost:3000

# Same Spotify Client ID as backend/.env — the mobile app needs it to build
# the Spotify authorize URL (the client secret stays backend-only).
EXPO_PUBLIC_SPOTIFY_CLIENT_ID=
